# Server Configuration
PORT=7001
BASE_URL=http://localhost:7001

# Encryption Key (32-byte hex string)
# Generate with: openssl rand -hex 32
# This is used to encrypt public configs (languages and API keys) for read-only access without passwords.
# If not set, a random key is generated on each restart, which means configs won't persist across restarts.
MASTER_KEY=your_master_key_here_32_bytes_hex

# OpenRouter API (Optional - fallback only)
# Users should configure their own API key via the web interface at /configure
# The server-side OPENROUTER_API_KEY is optional and only used as a fallback if a user hasn't configured their own key.
OPENROUTER_API_KEY=
OPENROUTER_REFERER=
